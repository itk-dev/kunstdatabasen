framework:
    default_locale: da
    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks:
            - da
#        providers:
#            crowdin:
#                dsn: '%env(CROWDIN_DSN)%'
#            loco:
#                dsn: '%env(LOCO_DSN)%'
#            lokalise:
#                dsn: '%env(LOKALISE_DSN)%'
