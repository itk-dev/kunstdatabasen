<div class="container">
    <div class="row art-details">
        <div class="col-md-4">
            <div id="carouselImageIndicators" class="carousel slide" data-interval="false">
                <div class="carousel-inner">
                    {% for image in item.images %}
                        <div class="carousel-item{% if loop.first %} active{% endif %} position-relative">
                            <a href="{{ image }}" target="_blank">
                                <img class="d-block w-100" src="{{ asset(image) }}" alt="Billede">
                                <span class="position-absolute bg-dark text-light rounded-circle py-1 px-2" style="right: 1em; bottom: 0.5em;" >
                                    <span class="sr-only">
                                        {{ 'image-carousel.expand'|trans }}
                                    </span>
                                    <i class="fas fa-expand"></i>
                                </span>
                            </a>
                        </div>
                    {% endfor %}
                </div>
            </div>
            <div class="d-flex justify-content-between mt-1">
                <a href="#carouselImageIndicators" role="button" data-slide="prev" style="color: black">
                    <span class="sr-only">{{ 'image-carousel.previous'|trans }}</span> <i class="fas fa-arrow-alt-circle-left" aria-hidden="true"></i>
                </a>
                <span><span id="carouselImageActiveIndex">1</span> / {{ item.images|length }}</span>
                <a href="#carouselImageIndicators" role="button" data-slide="next" style="color: black">
                    <span class="sr-only">{{ 'image-carousel.next'|trans }}</span> <i class="fas fa-arrow-alt-circle-right" aria-hidden="true"></i>
                </a>
            </div>
        </div>
        <div class="col-md-8">
            <div class="row">
                <div class="col-md-4">
                    <label for="detailsArtist">{{ 'frontend.item.artist'|trans }}</label>
                    <p>{{ item.artist ?? '' }}</p>
                </div>
                <div class="col-md-4">
                    <label for="detailsType">{{ 'frontend.item.type'|trans }}</label>
                    <p>{{ item.type ?? '' }}</p>
                </div>
                <div class="col-md-4">
                    <label for="detailsDimensions">{{ 'frontend.item.dimensions'|trans }}</label>
                    <p>{{ item.dimensions ?? '' }}</p>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <label for="detailsComment">{{ 'frontend.item.comment'|trans }}</label>
                    <p>{{ item.comment ? item.comment|nl2br : '' }}</p>
                </div>
            </div>
            <div class="row mt-3">
                <div class="col-12">
                    <h2 class="h4 border-bottom pb-2">{{ 'frontend.item.location'|trans }}</h2>
                </div>
                <div class="col-md-4">
                    <label for="detailsDepartment">{{ 'frontend.item.department'|trans }}</label>
                    <p>{{ item.department ?? '' }}</p>
                </div>
                <div class="col-md-4">
                    <label for="detailsBuilding">{{ 'frontend.item.building'|trans }}</label>
                    <p>{{ item.building ?? '' }}</p>
                </div>
                <div class="col-md-4">
                    <label for="detailsGeo">{{ 'frontend.item.geo'|trans }}</label>
                    <p>{{ item.geo ?? '' }}</p>
                </div>
            </div>
            <div class="row mt-3">
                <div class="col-12">
                    <h2 class="h4 border-bottom pb-2">{{ 'frontend.item.further_information'|trans }}</h2>
                </div>
                <div class="col-md-4">
                    <label for="detailsArtNo">{{ 'frontend.item.art_serial'|trans }}</label>
                    <p>{{ item.artNo ?? '' }}</p>
                </div>
                <div class="col-md-4">
                    <label for="detailsPrice">{{ 'frontend.item.purchase_price'|trans }}</label>
                    <p>{{ item.price ?? '' }}</p>
                </div>
                <div class="col-md-4">
                    <label for="detailsProductionYear">{{ 'frontend.item.production_year'|trans }}</label>
                    <p>{{ item.productionYear ?? '' }}</p>
                </div>
                <div class="col-md-4">
                    <label for="detailsEstimatedValue">{{ 'frontend.item.assessment_price'|trans }}</label>
                    <p>{{ item.estimatedValue ?? '' }}</p>
                </div>
                <div class="col-md-4">
                    <label for="detailsEstimatedValue">{{ 'frontend.item.assessment_date'|trans }}</label>
                    <p>{{ item.estimatedValueDate ?? '' }}</p>
                </div>
            </div>
        </div>
    </div>

    <div class="modal" tabindex="-1" role="dialog" id="carouselImageModal">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="btn" data-dismiss="modal" aria-label="{{ 'action.close'|trans }}">
                        <span aria-hidden="true"><i class="fas fa-times"></i></span>
                    </button>
                </div>
                <div class="modal-body">
                    <img class="img-fluid" id="carouselImageFull" src="" alt="{{ 'frontend.image_large'|trans }}">
                </div>
            </div>
        </div>
    </div>
</div>
