{% extends 'admin/base.html.twig' %}

{% block title %}{{ 'admin.item.new'|trans }}{% endblock %}

{% block body %}
    <div class="container-fluid bg-light">
        <div class="row">
            <div class="col">
                {% block content %}
                    <div class="container">
                        <div class="row">
                            <div class="col">
                                <h1 class="h4">{{ 'action.create_new_item'|trans }}</h1>
                                {{ include('admin/item/_form.html.twig') }}
                            </div>
                        </div>
                    </div>
                {% endblock %}
            </div>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}
    {{ encore_entry_link_tags('admin/new') }}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('admin/new') }}
{% endblock %}
