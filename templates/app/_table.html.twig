<div class="container-fluid">
    <div class="row">
        <div class="col">
            <div class="table-responsive">
                <table class="table table-borderless">
                    <thead>
                    <tr class="text-muted small">
                        <th></th>
                        <th>{{ 'frontend.artwork.title'|trans }}</th>
                        <th>{{ 'frontend.artwork.art_serial'|trans }}</th>
                        <th>{{ 'frontend.artwork.artist'|trans }}</th>
                        <th>{{ 'frontend.artwork.type'|trans }}</th>
                        <th>{{ 'frontend.artwork.dimensions'|trans }}</th>
                        <th>{{ 'frontend.artwork.building'|trans }}</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for art in artworks %}
                        <tr>
                            <td><a href="{{ art.link }}"><img src="{{ asset(art.img) }}" alt="" height="60"/></a></td>
                            <td><a href="{{ art.link }}"><strong>{{ art.title }}</strong></a></td>
                            <td>{{ art.artNo }}</td>
                            <td>{{ art.artist }}</td>
                            <td>{{ art.type }}</td>
                            <td>{{ art.dimensions }}</td>
                            <td>{{ art.building }}</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <div class="row justify-content-center">
        <div class="col-auto">
            {{ knp_pagination_render(pagination) }}
        </div>
    </div>
</div>
